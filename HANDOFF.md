# Food Passport 项目交接

记录日期：2026-09-21。目的：无需原聊天也能继续项目。这里记录用户决策和当前真实完成度，源码是实现事实，PRD 是业务依据。后续明确指令优先于历史方案；历史交付文档保留当时状态，不代表当前最终决策。

## 最新增量：饮食习惯第一页

用户随后指定饮食习惯选择为第一页，并提供示意图要求直接实现。新增 `/demo`，实现三组可共存多选、自定义添加/删除、继续后的可返回汇总弹窗。源码 `src/pages/DietPreferences.tsx`，示意图仅作为布局参考，样式受已有 Design System 约束，不允许把截图差异补成规范例外。继续目前仅确认本页选择，不是已实现下一页。此前“从人物优先”只是旧建议，接下来从此页延展流程。本次提交包含此增量，远端状态以 Git 为准。

## 2026-09-22 当前接手状态（优先于后文历史阶段说明）

- 首个产品原型已在 `/demo` 实现：浅色嘴部配置页作为入口，保存后进入饮食习惯选择页。嘴部配置包含鼻子、嘴型、牙齿、舌头和口腔颜色，并在后续人物动画中复用。
- iPhone 17 Pro 402×874；默认和全部预置选中可一屏展示。内容面板四边 32px，标签圆角 8px，选中无左侧图标；均引用既有 tokens。示意图只参考布局，不能为外部样式新增规范例外。
- 三类跨组多选；自定义添加后选中，取消选中即删除；预置标签取消选中仍保留。
- Add your own 原位平滑展开到组宽，输入与选中态背景/边框/文字相同，输入前后字号都是 14px。Enter、右侧勾号或非空失焦提交；空白失焦收起；Escape 放弃；按类别忽略大小写去重。
- 桌面模拟键盘支持字母/数字/Shift/删除/空格/Done/收起，内容区域让出键盘空间，输入保持可见；触控设备使用系统键盘，不重复模拟。物理 iPhone 输入法与 14px 输入是否触发 Safari 缩放仍需真机验证。
- Continue 目前打开选择汇总，可返回编辑；不是已经打通下一步。会话仅保存在内存，刷新重置。
- 源码 `src/pages/DietPreferences.tsx`、`SimulatedKeyboard.tsx`、`diet-preferences.css`；测试 `tests/diet-preferences.spec.ts`。当前完整测试集 10 项。
- 下一步在当前页基础上，按用户新的页面/流程指令推进；此前「尚未实现条件设置页」等描述是旧阶段记录。

## 1. 已确认的用户决策

- 产品主张：帮助国外用户在来到中国之后，面对未知的食物，能够快速识别是否能吃，涵盖是否过敏、是否合适、是否偏好及是否有饮食限制。
- iOS 优先；本阶段以 iPhone 浏览器体验为目标，不是原生 Swift 项目。
- 只做能完整演示全流程的 Demo；不接真实 AI、相机、麦克风、数据库、翻译、登录等服务，不以正式上架为当前交付目标。
- 技术栈已确认：React + TypeScript + Vite。已安装 React 19.3.0、Ant Design 6.0.0；以 package-lock.json 为锁定依据。
- 过敏、饮食限制、偏好三类可同时选择，每类内部多选；Tab 只切展示内容，不改变其他类别是否生效。
- 页面按照「主流程 → 分步页面 → 用户行为与体验」逐步构建，像场景电影；细节逐步确认，不预先锁死底部导航。
- 核心体验：拍照 → 食物成为动画贴纸 → 下方人物吃食物 → 满足微笑 / 夸张“死翘翘”式负向表达。尽量少文字，但保留短标签、关键原因和下一步。
- 加入人物配置，参考多邻国 Avatar Creator 的部件组合方式。参考链接：https://blog.duolingo.com/avatar-creator/ 。人物源代码为原创 SVG 示意，非复制官方资产。
- 主题最新决定为 **蓝色 #2563EB**，与多邻国区别。早期“多邻国配色/品牌绿”已被本次决定取代。悬停 #1D4ED8，按下 #1E40AF，白色按钮字；绿色只保留状态/插画作用。
- 其他基础 design tokens 直接复用 **antd 6.0.0**，不重建字号、间距、圆角、阴影与基础时间刻度。
- 用户要求客观理性，不要附和。沟通中文，规范站中文；产品英文主界面、店员简体中文 + 英文对照是 PRD 当前约定，最终文案仍需设计确认。

## 2. 阅读顺序

1. `PRD-Food-Passport-iOS-Demo.md`：v0.4 主流程、场景、匹配规则、验收和待确认。
2. `RULES.md`、`DEV-WORKFLOW.md`：实现边界与命令。
3. `design-system/README.md`、`foundations.md`、`components.md`、`avatar.md`、`motion.md`：规范。
4. `docs/delivery/design-system-audit.md`：最新完整性检查。
5. `src/design-system/handbook.ts` + `src/components/DesignerHandbook.tsx`：设计师手册，完整状态分类与实际主题数值。
6. `.qoder/project-adapter.md` 与 `.qoder/specs/`：项目适配和历史方案。
7. 下面列出的核心实现和测试。

原始聊天里粘贴的构想已归纳进本地 PRD；无需访问另一台机器的 attachments。此交接文档不是完整聊天逐字记录，未记录的细节不能推断为已确认。

## 3. 已有实现

### Design System

- `src/App.tsx`：中文规范站，栏目包括设计师手册、总览、颜色、排版、变量、组件、人物和约束。
- `/design-system#guide` 可直接打开手册；其他栏目使用 React 内部状态，不是业务路由。未引入路由库。
- `src/design-system/theme.ts`：品牌与语义色、Ant Design 主题、基础值对照。
- `src/design-system/TokenProvider.tsx`：主题映射 CSS 变量，无独立手抄刻度。
- `src/styles/global.css`：现有全站布局、响应式与动画；主要为紧凑样式，修改前阅读相关选择器。
- `public/favicon.svg` 与 `PassportIcon` 已统一蓝色。
- 手册展示完整性矩阵、12 个章节、实际颜色/字号/间距/圆角/控件/阴影/动效数值及待补内容，可调用浏览器打印。

### 人物和组件沙盒

- `src/components/Illustrations.tsx`：Avatar、FoodSticker、PassportIcon，人物为 240×280 半身 SVG。
- 人物配置：6 发型、2 脸型、2 肩型、6 肤色、5 发色、2 服装款式、6 服装色、无/圆/方眼镜、5 背景；4 预设晴天/栗子/阿洛/小满。
- `src/components/AvatarBuilder.tsx`：编辑草稿与已保存配置；即时预览、保存、取消、跨类保留；创建人物/反应预览两模式。
- 动作状态 idle / eating / match / conflict / unknown；减少动态时给静态结果，切换与离开取消计时。
- 当前负向仅倾斜与表情变化，不是已确认的最终倒地表演。
- `src/components/Playgrounds.tsx`：状态标签、跨类条件示例、按钮/切换/弹窗样例。中文沟通卡是固定花生示例，不是条件驱动的完整业务。
- 规范站人物配置只在当前沙盒保留；离开栏目会重置。Hero 与预设卡不读取用户保存人物。未来业务需提升会话状态，不能直接假设已有持久化。
- 食物仅有一份面条 SVG，不存在照片转换服务或全套菜品资源。

## 4. 尚未完成与不要误判的内容

完整性判断：基础足够开始逐页设计，但不能称为完整产品系统。

未实现：开场到设置再到拍照的完整链路、预制照片/贴纸一一对应素材、照片转贴纸转场、条件与样例驱动结果、原因展开、按条件组成的双语卡、模拟回复更新、会话记录与重置。

未最终确认：页面布局与导航、人物最终画风与部件范围、食物贴纸视觉、负向动作强度、偏好与过敏是否区分反应、未知动作、英文短标签和镜头节奏。脸型/肩型在沙盒里已扩展，但 PRD 初始范围将它们列为扩展；应在设计该页面时确认，不能说用户已经逐项批准。

还缺：完整控件状态展示、错误/空数据/素材失败场景、食材原因图标集、资产交付清单、Figma 可编辑组件库。Figma 不是继续当前项目的前置依赖。

原文链接已阅读用于风格参考，但之前官方配图加载不成功，未做逐像素比对；不能宣称已精准复刻官方人物。

## 5. 必须保留的业务规则

- 没设置个人条件，不输出个人化 OK；不能把未知当匹配。
- 明确冲突优先，其余未知仍保留；没有冲突但存在相关未知时待确认。
- 完整预设匹配才走正向；结果来自样例与三类已选条件，不是每道菜永远固定一个结局。
- 同时存在原因时突出过敏、再饮食限制、再偏好，不丢失其他原因。
- “店员说未添加”不自动清除共用锅具等未知，历史回答不自动适用新条件。
- 外观不推断饮食要求；预览与反应始终使用同一个用户人物。
- 当前未知静态示例不等于最终第三种表演已确认。完整主线要有跳过/重播；当前播放/重置按钮尚不能覆盖全部需求。
- 动画不是人体模拟或现实食物安全保证；Demo 标识清晰，关键原因不能全部藏起来。

## 6. 推荐下一步（尚未实施）

从 PRD 步骤 01–03 提出具体页面方案：是否单独开场、默认人物继续、保存与取消、三类条件选择与汇总；每次只确认一页或一组连续画面。设计时同步验证贴纸舞台（步骤 05–07）可沿用同一人物。

随后补模拟拍摄和明确的本地场景脚本，打通正向、过敏冲突、偏好不符、多条件共存、信息不足、沟通后变化六类场景，再接辅助流程。无需为了“系统完整”先开发大量不相关组件，也不要擅自接真实接口或发布。

用户对交接和推送已有授权；这不等于下一阶段所有页面与分镜已经批准。

## 7. 工程与验证

```sh
npm ci
npm run dev
npm run build
npm run typecheck
npm run check:tokens
npm run check:contrast
npm test
```

- 需 Node/npm；项目未声明 engines，换环境按锁定依赖要求选兼容版本，不猜原机器版本。
- Vite 默认端口 5173；Playwright 固定 127.0.0.1:5173，复用现有服务或自行启动。避免占用后自动跑到另一个端口。
- Playwright 使用 Google Chrome channel，需安装 Chrome。它是桌面浏览器/viewport 检查，不是 iPhone Safari 真机验收。
- 最近实现验证通过：build、typecheck、126 项非颜色 token 原值检查、10 组关键文本对比度、4 项浏览器测试（包含手册在 360/390/430/1440 宽度）。
- 构建有 >500kB 的 JS chunk 提示，未优化，不影响当前 Demo；不可声称没有任何 warning。
- 未完成真机 Safari、安全区、软键盘、字体放大和读屏全面验收；不能把部分无障碍检查描述为全面认证。
- `tests/design-system.spec.ts`：人物编辑/状态、分类共存、响应式、变量来源、减弱动态与焦点。`scripts/`：token 与对比度检查。
- `docs/delivery/` PNG 是评审快照；其中旧总览/旧人物截图可能保留早期绿色，不优先于当前实时蓝色主题。人物 v2 与手册截图较新。

## 8. 工作区与仓库

原工作区：`/Users/tree/Downloads/RED`。新电脑以克隆位置为准，所有文档使用相对路径。
远端：https://github.com/Tree080730/RED-Design 。原远端 main 只有提交 `bff7faa` 的 `# RED-Design` README；本次接手该历史并补项目，禁止强推覆盖历史。

不提交 node_modules、dist、测试临时产物或本机凭据。GitHub 身份验证属于新机器环境，不随代码转移。仓库提交包含 PRD、源代码、锁文件、规范、交接、脚本、测试与截图。分享链接部署尚未执行，本地服务也不会随 Git 自动迁移。

历史曾使用本机 meiling-lj-designer-dev-workflow skill 辅助规范整理；新 Agent 不需要有该本机 skill 才能读懂或运行项目，必要项目约束已沉淀进仓库。

## 最新设备目标

用户明确面向 App，基准 iPhone 17 Pro。当前仍是 React 交互原型，不代表原生 App 已打包。/demo 桌面画布为 402×874，手机使用真实 viewport 与安全区；页面可滚动。自定义标签和删除使用已有 controlHeightLG + marginXXS 组合点击区域；主操作恢复原生 Ant Design 大按钮。顶部与底部几何是原型预留，不宣称等于 iOS 所有运行状态的真实 inset。
