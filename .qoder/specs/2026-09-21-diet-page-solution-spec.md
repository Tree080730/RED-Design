# 饮食习惯第一页交互原型

用户提供示意图并明确要求直接实现。以饮食条件页作为当前第一页，取代之前建议的人物优先顺序。复用三类条件与英文清单，三组并列展示而非 Tab；支持跨组多选、自定义添加/移除和继续后的可返回汇总弹窗。后续业务页尚未确认，不伪造完整后续流程。

新增 /demo 路径，保留 /design-system。视觉以截图上方留白、圆角白面板、灰色胶囊和底部厚底按钮为依据；局部胶囊圆角与按钮底边为用户明确截图要求，不改全局 Ant Design tokens。颜色沿用已确认品牌蓝。无需新依赖。当前会话 React 状态，刷新重置。验证跨组保留、自定义去重/删除、零选择反馈、返回编辑和手机无横向溢出。

最新用户纠正：现有规范约束页面，禁止为外部示意图扩展例外。已撤销自定义胶囊、大圆角、阴影和厚底按钮；回归原有圆角/间距 token 与原生按钮。设备画布尺寸保留，不改全局规范。
