# 嘴部设置 · 五分类参考图

配置页 → 喜好页转场：Continue 后由同一配置人物连续移动到画面中央、张嘴并展开为喜好页背景。约 0–480ms 移动居中，480–950ms 放大到喜好页宽度并完成轮廓对齐，950–1550ms 在固定宽度和圆角下沿直线纵向张嘴；张嘴阶段不再插值曲线采样点。1450ms 起标题与三组选项、底部按钮依序淡入并上移 12px，2200ms 完成。喜好页 MouthFrame 与转场共用 mouthExpansion 的最终几何，保留鼻孔、牙齿、舌形、口腔颜色和发型；发型收拢到额头以避开标题。转场期间禁用页面交互，重复点击不会启动第二条时间轴；减少动态效果直接进入最终状态，窗口尺寸变化时结束转场并重新适配。设置卡片内容不参与转场变形。

耳朵全局更新：MouthEars 按最新参考图的脸宽比例统一耳高、向外伸出长度和垂直位置，采用横向圆头形状，放在脸部后方连接。最终预览、选项所用完整人物和 JourneyMouth（确认、咀嚼、两种结果）共用该组件；动画按实时脸框计算，不再用独立耳朵坐标与额外淡出。放大的结果人物耳朵随画面边界自然裁切。预览取景扩宽以完整显示耳朵。

预览一致性修正：上方改用 FinalMouthPortrait 组合最终人物。鼻形、嘴形直接复用选项几何；牙齿以 Teeth Style 卡片的 x=0、y=66、width=400、height=38 为标准，金牙、龅牙、蛀牙和缺口整体等比变换，不再分别手调尺寸。口腔内层对舌头进行裁剪，下排白色牙带保持前景，金牙不再被口腔轮廓斜切。圆形嘴的牙带根据弧形边界定位。下方保留已确认的各自取景与隐藏规则；五个默认设置面板修改前后的浏览器截图逐字节一致。检查了五种牙齿、四种嘴形及舌头/头发组合的实际截图；构建、类型、token、对比度与 14 项浏览器测试通过。本轮仅调整设置页上方预览，不修改后续动画场景。

嘴形选项细化：按 Mouth Shape 局部图绘制专用 SVG 缩略图，展示圆角横口、正圆、平顶笑口、方口。选项缩略图隐藏牙齿、舌头、耳朵及头发以突出嘴形；保留鼻子与脸部轮廓。Mouth Color 色块圆角与下部轻微提亮按参考调整，选择仍实时更新上方完整预览。

Tab 栏细化：按用户 1534px 宽局部参考重新绘制五个 SVG 图标，保留各图标的独立宽高与视觉位置；栏高按 1534:254 比例缩放，402px 画布下约 66.56px。灰色轮廓、蓝色选中轮廓、2px 底线和占单格全宽的下划线均与参考对齐。截图检查使用 3 倍像素密度。

2026-09-24：根据用户五张图重做 `/demo` 起始设置页。标题 Create your MOUTH；全宽浅蓝预览舞台；鼻子、嘴、牙齿、舌头、头发五个原创 SVG 图标和蓝色下划线；各分类两排横向滑动缩略图；继续按钮固定。

鼻子第二排按参考图保留标题 Nose Color，卡片内容实际表达鼻孔形状（线、圆点、星形等）。五个分类的缩略图按局部参考图分别复原，不能用上方完整预览的裁剪结果替代；上方预览与缩略图共享同一份配置状态，但各自保留参考图规定的构图。头发为本次明确新增的配置。牙齿增加可取消的贴纸；颜色和形状即时更新矢量预览，切换分类保留选择。SVG 裁切 id 唯一，避免多个缩略图互相覆盖。口腔颜色并入 Mouth，不再单设 Inside。

402×874 画布上下保留 44px。页面外层不滚动，选项面板可独立滚动，卡片行横向滑动。底部占位文案替换为 Continue；主操作保留项目品牌蓝。

验证：构建、类型、126 项 token、10 组文本对比度和 14 项浏览器测试通过。测试包含五分类切换、选项保留、继续进入饮食条件流程。构建保留既有包体积提示。

范围：此轮完成设置页与配置预览。后续动画尚未完整采用头发、舌形、嘴型/大小，不能宣称所有外观字段在所有场景同步。
