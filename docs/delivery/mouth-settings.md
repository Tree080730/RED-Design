# 嘴部设置 · 五分类参考图

嘴形选项细化：按 Mouth Shape 局部图绘制专用 SVG 缩略图，展示圆角横口、正圆、平顶笑口、方口。选项缩略图隐藏牙齿、舌头、耳朵及头发以突出嘴形；保留鼻子与脸部轮廓。Mouth Color 色块圆角与下部轻微提亮按参考调整，选择仍实时更新上方完整预览。

Tab 栏细化：按用户 1534px 宽局部参考重新绘制五个 SVG 图标，保留各图标的独立宽高与视觉位置；栏高按 1534:254 比例缩放，402px 画布下约 66.56px。灰色轮廓、蓝色选中轮廓、2px 底线和占单格全宽的下划线均与参考对齐。截图检查使用 3 倍像素密度。

2026-09-24：根据用户五张图重做 `/demo` 起始设置页。标题 Create your MOUTH；全宽浅蓝预览舞台；鼻子、嘴、牙齿、舌头、头发五个原创 SVG 图标和蓝色下划线；各分类两排横向滑动缩略图；继续按钮固定。

鼻子第二排标题按用户更正为 Nostril Shape（鼻孔形状）。两排缩略图按局部参考校准：圆角鼻、圆弧鼻、尖角鼻；鼻孔包含圆头竖线、圆点、圆角五角星。鼻孔用共享 SVG 几何绘制，在缩略图和大预览同步，不依赖字体星形。头发为本次明确新增的配置。牙齿增加可取消的贴纸；颜色和形状即时更新矢量预览，切换分类保留选择。卡片使用真实部件渲染且 SVG 裁切 id 唯一，避免多个缩略图互相覆盖。口腔颜色并入 Mouth，不再单设 Inside。

402×874 画布上下保留 44px。页面外层不滚动，选项面板可独立滚动，卡片行横向滑动。底部占位文案替换为 Continue；主操作保留项目品牌蓝。

验证：构建、类型、126 项 token、10 组文本对比度和 14 项浏览器测试通过。测试包含五分类切换、选项保留、继续进入饮食条件流程。构建保留既有包体积提示。

范围：此轮完成设置页与配置预览。后续动画尚未完整采用头发、舌形、嘴型/大小，不能宣称所有外观字段在所有场景同步。
