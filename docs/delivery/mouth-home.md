# 嘴巴容器首页

用户提供新首页构图，要求将饮食选择置入大嘴巴内。新增 MouthFrame SVG：肤色外框与鼻子、上下白色牙齿、深红口腔、底部舌头，作为 HTML 控件背后的装饰层。首页不再显示独立头部，原 WelcomeHead 组件保留。

控件沿用既有字号、8px 圆角、44px 自定义点击区域与主题间距。口腔背景下使用白字与半透明白色标签底，标题使用现有浅蓝；选中与行内输入仍使用原浅蓝语义样式。Continue 移至容器外。色块是插画配色，不修改全局主题。

保留条件共存、自定义取消即删除、原位展开输入、模拟键盘、已有模糊背景确认面板。修正确认遮罩与返回按钮同名导致的定位歧义。确认页仍是当前会话预览，后续业务页未新增。

预览 /demo，402×874。截图 diet-mouth-home.png。

最新布局：面部横向贴屏，口腔（含白色牙齿）距面部四边均为 32px，内容距离口腔左右各 16px 并填充可用宽度。顶部和底部各预留 44px，若设备系统安全区更大则使用系统值。新增问题标题 “What do you avoid?” 和 44px 点击区域的返回按钮；编辑时返回收起输入，否则返回浏览器上一页，无历史时提示当前为 Demo 第一步。

402×874 浏览器实测口腔四边均为 32px，上下预留均为 44px，默认内容无滚动；已检查返回导航。真实 iPhone Safari 安全区与系统键盘仍待真机验证。
